# 5300-Giraffe
# Sprint 1 - Cheng and Bruno

# Sprint 1 Status: Incomplete
 Compiles/builds but doesn't run test_heap_storage() correctly

# how to build
run "make clean" in command line
run "make" in command line

# Errors
When running test_heap_storage(), 
traced segmentation fault to SlottedPage::address() using cout

Possible errors in HeapTable::validate because not all Python code was included
